# shared-ui

Reusable Angular components, signals-based services, theme tokens, and i18n helpers consumed by `shell-app`, `simulation-mfe`, and `export-mfe`.

Not a federated remote — published as a workspace library and consumed via path mapping during development.
