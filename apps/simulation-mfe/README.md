# simulation-mfe

Live + Capture mode UI. Native Federation remote exposed as `./Component` for the shell to load.

```bash
npm install
npm run start    # http://localhost:4201
```

Standalone usage (without shell): `http://localhost:4201` opens the component directly.
