# shell-app

Native Federation host for the astro micro-frontends. Routes between Simulate, Export, and About; loads the corresponding MFE on demand.

```bash
npm install
npm run start    # http://localhost:4200
```

Remote URLs are configured in `federation.manifest.json` (created during `ng add @angular-architects/native-federation`).
