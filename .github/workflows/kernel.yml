name: kernel

on:
  push:
    branches: [main]
    paths:
      - 'kernel/**'
      - '.github/workflows/kernel.yml'
  pull_request:
    paths:
      - 'kernel/**'

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install gfortran + HDF5 (macOS)
        if: runner.os == 'macOS'
        run: brew install gcc hdf5

      - name: Install gfortran + HDF5 (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y gfortran libhdf5-dev

      - name: Build kernel
        run: make -C kernel

      - name: Run kernel tests
        run: make -C kernel test
